/** D38, and mirrored in the sidecar's provider registry. */
export const DEFAULT_PROVIDER_ID = 'openrouter'

/** OpenRouter's router across whatever is free today, so a first question works. */
export const DEFAULT_MODEL_ID = 'openrouter/free'
